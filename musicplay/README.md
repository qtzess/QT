# musicplay

Qt Widgets music player demo.

## Build

Open `musicplay.pro` with Qt Creator, select a Qt 5.12 MinGW kit, then run qmake and build.

## Notes

Local `.mp3` files are ignored by Git. Put your own test music in the `music/` directory, or update the path in `widget.cpp`.
